# Xを落とす
sudo service gdm stop
# Ctrl+Alt+F1でログインし、ドライバをインストール
sudo ./cuda_6.5.14_linux_64.run -silent -driver
# 再起動してXを立ち上げ、ツールキットとサンプルをインストール
sudo ./cuda_6.5.14_linux_64.run

# .bashrcなどに以下の２行を追加
	export PATH=/usr/local/cuda-6.5/bin:${PATH}
	export LD_LIBRARY_PATH=/usr/local/cuda-6.5/lib64:/usr/local/cuda-6.5/lib:${LD_LIBRARY_PATH}

# デフォルトでは$HOMEにインストールされるサンプルをコンパイル
cd ~/Nvidia_CUDA_xxx_Samples
make

# opencvをcmakeでWITH_CUDA=ONにして実行、make、make install
# 2.4.9では失敗、2.4.10では成功した(2014/11/15)
